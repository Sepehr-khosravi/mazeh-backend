#node modules (never compy these)
node_modules
npm-debug.log
yarn-error.log

#build output
dist
.prisma

#env files never put secrets in image
# .env
# .env*
# =!.env.example

#git repo
.git.gitignore

#Editor filters
.vscode

#Dockerfile (not needed inside the image)
Dockerfile
docker-compose.yml

#logs
logs
*.log

#Docker cache & temp
tmp
.cache
